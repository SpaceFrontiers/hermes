# Benchmark evidence inventory

Reports, plots and SHA-256 manifests are versioned here. Raw ZIP archives are
local benchmark evidence and are excluded from Git; archive paths in these
reports identify files in the originating workspace, not downloadable release
assets. The manifests retain archive and member hashes for verification.

Current implementation decisions and measurement limitations are recorded in
[the performance review](../search-performance-review.md).
